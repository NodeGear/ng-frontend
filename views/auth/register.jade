.col-md-4.col-md-offset-4.col-lg-4.col-lg-offset-4.col-sm-6.col-sm-offset-3.col-xs-10.col-xs-offset-1
	h3.text-center NodeGear
	p.text-center Register an Account
	form(name="register" ng-init="init('#{token}')")
		fieldset
			p.text-center(ng-cloak ng-show="status.length > 0" ng-bind="status")
			.form-group
				input.form-control(placeholder="What's your Name?" type="text" autofocus="" required ng-model="user.name")
			.form-group
				input.form-control(placeholder="Pick a Username" type="text" required ng-model="user.username")
				p.help-block(ng-show="help_text.username.length > 0") {{ help_text.username }}
			.form-group
				input.form-control(placeholder="Enter a valid Email Address" type="email" required ng-model="user.email")
				p.help-block(ng-show="help_text.email.length > 0") {{ help_text.email }}
			.form-group
				input.form-control(placeholder="Choose a strong Password" type="password" required ng-model="user.password")
			.form-group
				button.btn.btn-lg.btn-success.btn-block(ng-disabled="register.$invalid" ng-click="registerUser()")
					i.fa.fa-key.fa-fw
					|  Register
		p.text-center
			| I have an account, take me to the 
			a.link-black.link-underline(ui-sref="login") Login page