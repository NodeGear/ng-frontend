.col-md-4.col-md-offset-4.col-lg-4.col-lg-offset-4.col-sm-6.col-sm-offset-3.authentication
	h1.text-center NodeGear
	p.lead.text-center Register for a NodeGear Account

.col-md-4.col-md-offset-4.col-lg-4.col-lg-offset-4.col-sm-6.col-sm-offset-3.authentication-box.nodegear-slide-in-left
	form(name="register" ng-init="init('#{token}')")
		fieldset
			p.text-center(ng-cloak ng-show="status.length > 0" ng-bind="status")
			.form-group(style="margin-bottom:10px;")
				input.form-control.input-lg.text-center(placeholder="Full Name" type="text" name="full_name" autofocus="" required ng-model="user.name")
			.form-group(style="margin-bottom:10px;" ng-class="{ 'has-error': help_text.username.length > 0 }")
				input.form-control.input-lg.text-center(placeholder="Username" type="text" required ng-model="user.username")
				span.input-icon.fui-question(ng-show="help_text.username.length > 0" title="{{ help_text.username }}")
			.form-group(style="margin-bottom:10px;" ng-class="{ 'has-error': help_text.email.length > 0 }")
				input.form-control.input-lg.text-center(placeholder="Email Address" type="email" required ng-model="user.email")
			.form-group
				input.form-control.input-lg.text-center(placeholder="Password" type="password" required ng-model="user.password")
			.form-group
				button.btn.btn-lg.btn-success.btn-block(ng-disabled="register.$invalid" ng-click="registerUser()")
					i.fa.fa-key.fa-fw
					|  Register
		p.text-center: small
			| I have an account, take me to the 
			a(ui-sref="login") Login page